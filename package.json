{
  "name": "yatal",
  "version": "0.0.1",
  "description": "Yet another type assertion library.",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "test": "tsc -p ./tsconfig.test.json --noEmit",
    "prettier": "prettier '(src|tests)/**/*.ts' --write"
  },
  "keywords": [
    "typescript",
    "test",
    "expect",
    "assertion"
  ],
  "author": "Adrien Gautier",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
